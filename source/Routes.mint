routes {
  / {
    Application.setPage(Page::Home)
  }

  /about {
    Application.setPage(Page::About)
  }

  /services {
    Application.setPage(Page::Services)
  }

  /blog {
    Application.setPage(Page::Blog)
  }
}
